# 开发与版本管理

## 分支与提交

- `main` 保存可追溯的项目基线；非平凡实现从 `feat/<slug>`、`fix/<slug>` 或 `research/<slug>` 分支开展。
- 每次提交聚焦一个完整改动，采用 `docs:`、`feat:`、`fix:`、`test:`、`chore:` 等清晰前缀。
- 文档方案原地更新，不追加多份“最新方案”；用 Git 历史追溯设计变化。
- 显式暂存本次涉及的文件，提交前检查暂存差异，提交后确认工作区状态。不夹带其他工作。
- 原型尚未通过真机验证时如实记录状态，不以提交或构建成功替代功能验证。

## 依赖与产物

- 固定上游版本；如果使用 submodule，提交其精确引用及 `.gitmodules`。
- Rust 可执行项目引入后提交 `Cargo.lock`；其他包管理器的锁文件同样纳入版本管理。
- 构建目录、运行时设备配置、凭据、原始会话和未筛选的实验产物不入库。
- 精选证据放入 `docs/evidence/`，记录日期、设备/软件版本、执行方式、结果与证明边界。安装包记录来源和校验值，不直接提交大体积二进制。

## 文档与验证

功能方案统一位于 `docs/plans/`，每个功能一份当前设计。新增模块时同步维护 README 和可执行验证命令；运行与改动相称的检查。真实设备动作必须定向到明确序列号，证据日志不记录密码、按键内容或令牌。

## 公开仓库的隐私约定

- 示例只使用 `<ADB_SERIAL>` 等占位符，不提交真实设备序列号、个人路径、邮箱、内部项目或会话标识。
- 原始日志、截图、会话转录、安装包和历史备份仅留在被忽略的本地目录。公开证据以人工核对过的文字摘要为主。
- 提交作者使用 GitHub noreply 邮箱；发布前同时检查文件内容和可达 Git 历史。
